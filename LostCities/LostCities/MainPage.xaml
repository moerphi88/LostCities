<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:LostCities"
             xmlns:viewModel="clr-namespace:LostCities.ViewModel;assembly=LostCities"
             x:Class="LostCities.MainPage">
    <StackLayout>
        <StackLayout>
            <Label Text="Ablagestapel" FontSize="Large"></Label>
            <StackLayout 
                x:Name="stack"
                Orientation="Horizontal"
                Padding="10"
                HorizontalOptions="Start"
                VerticalOptions="Start"> 
                <!-- Hier kann ich mit Rotation die Buttons drehen -->
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding GelberStapelImageUri}" Command="{Binding OnButtonPressedCommand}" CommandParameter="0"/>
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding BlauerStapelImageUri}" Command="{Binding OnButtonPressedCommand}" CommandParameter="1"/>
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding GruenerStapelImageUri}" Command="{Binding OnButtonPressedCommand}" CommandParameter="2"/>
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding RoterStapelImageUri}" Command="{Binding OnButtonPressedCommand}" CommandParameter="3"/>
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding WeißerStapelImageUri}" Command="{Binding OnButtonPressedCommand}" CommandParameter="4"/>
            </StackLayout>
        </StackLayout>
        <StackLayout>
            <Label Text="Anlegestapel" FontSize="Large"></Label>
            <StackLayout 
                x:Name="Anlegestapel"
                Orientation="Horizontal"
                Padding="10"
                HorizontalOptions="Start"
                VerticalOptions="Start">
                <!-- Hier kann ich mit Rotation die Buttons drehen -->
                <Button WidthRequest="50" HeightRequest="70"  Image="{Binding ShownCardImageUri}" IsEnabled="False"/>
            </StackLayout>
        </StackLayout>
        <StackLayout>
            <Label Text="Spieler 1 Hand" FontSize="Large"></Label>
            <StackLayout 
                x:Name="HandSpielerEins"
                Orientation="Horizontal"
                Padding="10"
                HorizontalOptions="Start"
                VerticalOptions="Start">
            </StackLayout>
        </StackLayout>
        <StackLayout>
            <Label Text="Spieler 2 Hand" FontSize="Large"></Label>
            <StackLayout 
                x:Name="HandSpielerZwei"
                Orientation="Horizontal"
                Padding="10"
                HorizontalOptions="Start"
                VerticalOptions="Start">
            </StackLayout>
        </StackLayout>
    </StackLayout>
</ContentPage>